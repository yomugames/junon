{
  "name": "junon",
  "scripts": {
    "client": "npm run client --workspace packages/junon-io",
    "server": "nodemon --inspect start_server.js"
  },
  "workspaces": ["packages/*"],
  "nodemonConfig": {
    "ext": "js, json, proto"
  }
}
